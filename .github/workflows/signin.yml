name: Cloud Studio Daily Sign-in

on:
  workflow_dispatch:
    # 手动触发
  schedule:
    # 每天北京时间 09:00 执行
    # Cron: 分 时 日 月 周
    - cron: "8 8 * * *"  # UTC 01:00 = 北京时间 09:00

jobs:
  signin:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run sign-in
        env:
          CLOUD_STUDIO_COOKIES: ${{ secrets.CLOUD_STUDIO_COOKIES }}
          CLOUD_STUDIO_XSRF_TOKEN: ${{ secrets.CLOUD_STUDIO_XSRF_TOKEN || '' }}
          SERVERCHAN_KEY: ${{ secrets.SERVERCHAN_KEY || '' }}
        run: |
          python signin.py
